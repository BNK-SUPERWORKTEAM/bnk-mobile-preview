# 참고 영상 인터랙션 분석

원본: `~/Desktop/화면 기록 2026-09-18 오후 12.05.08.mov`
36.65s / 1918×990 / H.264 60tbr — 정상 재생 확인.
레퍼런스 사이트: AKARU (akaru.fr) — 리옹 웹에이전시.

## 0. 레이아웃 (핵심 정정)
Hero는 "좌우 분할"이 아니라 **풀블리드 슬라이더 위에 크림색 텍스트 카드가 오버레이**된 구조.
- 바닥 레이어: 프로젝트 슬라이더 (항상 전체 폭). 우측 가장자리에 다음 슬라이드 배경색이 세로 띠로 peek.
- 위 레이어: 크림색 카드(좌측, 화면 폭의 약 62%) — 상단 내비 / 대형 로고 타이포 / 카피 / 하단 SNS 4개.
- 슬라이드 하단 라벨바(연도 pill · 태그 · 프로젝트명 · 인덱스 · ↗버튼)는 카드 **뒤**에 깔려 있고,
  카드가 비켜날수록 드러남. 라벨 텍스트가 "가려졌다 드러나는" 느낌의 정체는 이것.

## 1. 페이지 전환 로더 (7.9s ~ 9.0s, 총 약 1.1s)
1. 크림 → **블랙으로 즉시 컷** (페이드 없음, 1프레임 미만)
2. 흰 세로 바 5~6개. 각 바는 높이·y오프셋이 제각각(계단형 배치), 세트가 좌→우로 스윕. 약 0.25s 주기 2회.
3. 크림 패널이 **오른쪽에서 들어와 왼쪽으로** 블랙을 밀어냄. 경계는 수직이 아니라 **살짝 기울어진 사선(skew)**.
   리빌 소요 약 0.4~0.5s. 뒤의 Hero는 이미 렌더된 상태 → translate가 아니라 **마스크 리빌**.
4. 리빌 후 동일 Hero 복귀.

> 내비 아래 프로그레스 라인: 상단 130px 전 구간을 프레임 단위로 훑었으나 확인 안 됨.
> 블랙 전환은 예고 없이 즉시 발생. (1~2px 초저대비면 압축에 묻혔을 수는 있음)

## 2. Hero ↔ 풀블리드 슬라이더
- 스크롤 = 크림 카드가 **좌측으로 translateX**. 슬라이더는 원래부터 풀블리드라 확장이 아니라 "드러남".
- 역스크롤 시 카드가 좌측에서 다시 들어옴. 양방향 가역.
- 슬라이드 전환: 이미지 + 배경색 + 라벨바가 **한 덩어리로 가로 translate**. 이전 슬라이드는 오른쪽으로 밀려남.
- 프로젝트: 01 Archidomo(블루그레이) / 02 Orlinski(모브핑크) / 03 Pikko(세이지그린).
- 스크롤 후 내비가 교체됨: 좌상단 소형 로고마크 + 우상단 `MENU ●` pill.

## 3. "Projets" 리스트 섹션
- 우측: `Projets` 타이틀 + 리드 문단 + 00~03 넘버드 리스트(얇은 hr, 우측정렬 2줄). 스크롤 중 고정(sticky).
- 좌측 세로 이미지 컬럼: **오토스크롤 아님.** 스크롤바 궤적과 대조한 결과,
  스크롤이 멈춘 30~37s 구간에서 좌측 컬럼도 완전히 정지.
  → 스크롤 연동(sticky 우측 + 좌측 컬럼이 다른 속도로 흐르는 패럴랙스)이 정확.
- 리스트 항목 hover 인터랙션 여부는 이 영상으로 확인 불가(커서가 올라간 장면 없음).

## 4. 우리 프로젝트 매핑
슬라이드 3장 = 바루랩 / 탐뷰티 / 피더린. 각 브랜드 컬러를 슬라이드 배경색으로.
